specVersion: "v1beta"
serviceId: "my_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
